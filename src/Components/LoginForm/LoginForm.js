import React from 'react';
import styles from './LoginForm.module.scss'

const LoginForm = () => {
    return (
        <div className={styles.login_form}>login form</div>
    )
}

export default LoginForm;